<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/static/style.css">
</head>
<body>
    <header>
        <h1>Cat Pictures</h1>
    </header>
    <main>
        <div class="gallery">
            <% data.forEach(cat => { %> 
                <a id=<%= cat.id %> href="/cat/<%= cat.id  %>">
                <img id=<%= cat.id %> width="500" height="500" src=<%= cat.src %> alt=<%= cat.name %> />
            </a>
            <% }) %>
        </div>
    </main>
    <footer>
        <p>&copy; 2024 Cat Lovers</p>
    </footer>
</body>
</html>